@model IList<LogEntryViewModel>

<div class="starter-template">
<h1>Daily Log</h1>
    <p class="lead">
        <table class="table">
            <tr>
                <th>Name</th>
                <th>Login</th>
                <th>Logout</th>
                <th>Action</th>
            </tr>
            @foreach(var log in Model)
            {
                <tr>
                    <td>@log.Name</td>
                    <td>@log.StartTime</td>
                    <td>@log.StopTime</td>
                    <td><button class="btn btn-danger"><a asp-controller="Log" asp-action="Delete" asp-route-id="@log.data.Id">Delete</a></button></td>
                </tr>
            }    
        </table>
    </p>
</div>
